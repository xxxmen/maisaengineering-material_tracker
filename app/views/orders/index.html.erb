<style type="text/css" media="screen">
    .img_sort a { padding: 2px; }
    .img_sort img { display: none; }
    .img_sort a img { display: inline; }
    * html #maininfo { height: expression((document.body.clientHeight-242).toString() + 'px'); }   /* make it a bit smaller due to large header */
</style>

<%= render :partial => 'refine' %>

<% if warehouse_view? %>
    <%= render :partial => "warehouse" %>
<% elsif receiving_view? %>
    <%= render :partial => "receiving" %>
<% else %>
    <%= render :partial => 'purchasing' %>
<% end %>

<%= render 'shared/will_paginate', collection: @orders %>

<%#= render :partial => "shared/paginate", :locals => { :collection => @orders } %>